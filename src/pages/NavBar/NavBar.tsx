import { NavBarContainer, NavBarLogo, NavBarMenu } from "./NavBarComponents"

export const NavBar = () => {
    return(
        <NavBarContainer>
            <NavBarLogo/>
            <NavBarMenu>
                
            </NavBarMenu>
        </NavBarContainer>
    )
}